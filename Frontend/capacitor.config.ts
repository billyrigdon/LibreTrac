import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tech.welledumucated.app.libretrac',
  appName: 'libretrac',
  webDir: 'dist/libretrac',
  bundledWebRuntime: false
};

export default config;
